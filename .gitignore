*_private_key
*_public_key
*_preshared_key
*.key
*.params.json
*.swp
*.bak
.vscode
*.code-workspace
# Kiro workspace (steering, specs, agent config) — internal context kept out of
# this public repo. Steering lives at .kiro/steering/ only on local clones.
.kiro/
wireguard-*.conf
confs/
keys/